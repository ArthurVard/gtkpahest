<%= form_for(@asset) do |f| %>
  <% if @asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@asset.errors.count, "error") %> prohibited this asset from being saved:</h2>

      <ul>
      <% @asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <strong>Անուն</strong><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <strong>Հաստատագրված քանակ</strong><br>
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <strong>Նշում</strong><br>
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit 'Հիշել', class: "btn btn-primary"%>
  </div>
<% end %>
