<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <strong>Թիվ</strong><br>
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <strong>Անուն</strong><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <strong>Նշւոմ</strong> <br>
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit 'Հիշել', class: "btn btn-primary" %>
  </div>
<% end %>
